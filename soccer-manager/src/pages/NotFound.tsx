import { Alert } from "@mui/material";

function NotFound() {
  return (
    <main>
      <Alert variant="standard" color="error">
        Not found
      </Alert>
    </main>
  );
}

export default NotFound;
